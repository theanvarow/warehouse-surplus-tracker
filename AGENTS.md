# Project Instructions & Deployment Rules

- **Avtomatik Deploy Qoidasi:** Har bir kodga kiritilgan o'zgarish va yangilanishdan so'ng darhol `npm run build` orqali tekshirilishi, `git add .`, `git commit -m "..."` qilinishi va `git push origin main` orqali GitHub / Render ga avtomatik deploy qilinishi shart.
